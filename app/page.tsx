'use client';
import {BookOpen,Compass,ArrowLeft,ArrowRight,Pause,Play,Info,MapPin,Globe2,MoveRight} from 'lucide-react';
import World from '@/game/World';
import Timeline from '@/game/Timeline';
import EarthMap from '@/game/Map';
import Hud from '@/game/Hud';
import Panels from '@/game/Panels';
import Audio from '@/game/Audio';
import {useWebMCP} from '@/game/useWebMCP';
import {useExpedition} from '@/game/useExpedition';
import {regions,periods} from '@/game/data';
import {NativeSelect,NativeSelectOption} from '@/components/ui/native-select';
export default function Game(){const g=useExpedition();useWebMCP(g);return <main className={`${g.started?'playing':'landing'} ${g.traveling?'traveling':''}`}><World period={g.period} region={g.region} active={g.active} onExplore={g.explore} worldRef={g.world} direction={g.direction}/><div className="vignette"/><header><button className="brand" onClick={()=>g.setPanel('help')} aria-label="Earth Through Time — how to play"><Globe2 size={23}/><span>EARTH THROUGH TIME</span></button><div className="header-right">{g.started&&<><button className="journal-button" onClick={()=>g.setPanel('journal')}><BookOpen size={15}/><span>Field journal</span><b>{g.visited.length}<small>/28</small></b></button><button className="icon-button" aria-label={g.paused?'Resume expedition':'Pause expedition'} onClick={()=>g.setPaused(!g.paused)}>{g.paused?<Play size={16}/>:<Pause size={16}/>}</button></>}<Audio biome={g.period.biome} paused={g.paused}/><button className="icon-button" aria-label="How to play" onClick={()=>g.setPanel('help')}><Info size={17}/></button></div></header>{!g.started?<><section className="start"><span className="eyebrow"><span className="tiny-line"/> AN EXPEDITION THROUGH DEEP TIME</span><h1>Earth<br/><em>through time.</em></h1><p>4.5 billion years. One planet. Endless worlds.</p><button className="primary" onClick={g.begin}>Begin journey <MoveRight size={20}/></button><button className="text-button" onClick={()=>{g.begin();g.travel(0)}}>Explore from the beginning <span>↗</span></button></section><div className="landing-caption"><span className="eyebrow">A WINDOW INTO THE LATE JURASSIC</span><p>150 million years before you.</p></div><footer><span>WALK INTO THE PAST. FIND YOUR PLACE IN IT.</span><button className="text-button" onClick={()=>g.setPanel('credits')}>Science & credits ↗</button></footer></>:<><Timeline index={g.index} onChange={g.travel}/><section className="period-title" aria-live="polite"><span className="eyebrow"><span className="tiny-line"/>{g.period.era.toUpperCase()} · {g.period.date.toUpperCase()}</span><h1>{g.period.name}</h1><p>{g.period.biome==='jurassic'?'Before us, a world of giants.':g.period.climate+' · '+g.period.vegetation}</p></section><aside className="map-panel"><div className="map-label"><Compass size={14}/><span>YOUR PLACE ON EARTH</span></div><EarthMap period={g.period} region={g.region}/><div className="region-picker"><MapPin size={12}/><NativeSelect aria-label="Starting region" value={g.regionIndex} onChange={e=>g.changeRegion(Number(e.target.value))}>{regions.map((r,i)=><NativeSelectOption value={i} key={r.id}>{r.name}</NativeSelectOption>)}</NativeSelect></div></aside><div className="exploration"><span className="eyebrow">EXPEDITION {String(g.index+1).padStart(2,'0')}</span><span>{Math.round(g.distance)} m explored</span><div className="route-progress"><i style={{width:`${Math.min(100,g.distance/55)}%`}}/></div></div><div className="world-actions"><div className="walk-controls"><button aria-label="Walk left" onPointerDown={e=>{e.currentTarget.setPointerCapture(e.pointerId);g.startWalking(-1)}} onPointerUp={()=>g.direction.current=0} onPointerCancel={()=>g.direction.current=0} onLostPointerCapture={()=>g.direction.current=0}><ArrowLeft size={17}/></button><button aria-label="Walk right" onPointerDown={e=>{e.currentTarget.setPointerCapture(e.pointerId);g.startWalking(1)}} onPointerUp={()=>g.direction.current=0} onPointerCancel={()=>g.direction.current=0} onLostPointerCapture={()=>g.direction.current=0}><ArrowRight size={17}/></button><span><kbd>A</kbd><kbd>D</kbd> TO EXPLORE</span></div><button className="investigate" disabled={!g.active} onClick={g.trigger}><Compass size={16}/><span>{g.world.current.nearby?'Observe wildlife':'Investigate surroundings'}</span><kbd>E</kbd></button><button className="next-era" onClick={()=>g.travel((g.index+1)%periods.length)}>{g.index===27?'Return to origins':'Next world'}<ArrowRight size={16}/></button></div><Hud period={g.period} region={g.region} stats={g.stats} onInfo={()=>g.setPanel('notes')}/>{(g.paused||g.stats.health<=0)&&!g.panel&&!g.encounter&&<div className="pause-screen"><span className="eyebrow">{g.stats.health<=0?'THE EXPEDITION ENDS. CURIOSITY DOES NOT.':'TAKE YOUR TIME'}</span><h2>{g.stats.health<=0?'A difficult world.':'Expedition paused.'}</h2><p>{g.stats.health<=0?'Your discoveries are safe in your journal. Try again with fresh supplies or travel onward.':'Your supplies are safe while you pause.'}</p><button className="primary" onClick={()=>g.stats.health<=0?g.retry():g.setPaused(false)}>{g.stats.health<=0?'Try this world again':'Resume expedition'}<ArrowRight size={17}/></button>{g.stats.health<=0&&<button className="text-button" onClick={()=>g.travel((g.index+1)%periods.length)}>Travel to the next world →</button>}</div>}{g.traveling&&<div className="travel-flash" aria-hidden="true"><span>TRAVELING THROUGH DEEP TIME</span></div>}</>}<Panels game={g}/></main>}
